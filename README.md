# OLS_coeffs_walkthrough
Simple calculation of the OLS coefficients only using numpy as an external library.

To follow along with the calculations, you need to have installed:

numpy (mandatory)

pandas (optional)
matplotlib (optional)